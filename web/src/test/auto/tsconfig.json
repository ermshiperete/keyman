{
  "extends": "../../tsconfig.dom.json",

  "compilerOptions": {
    "outDir": "../../../build/test",
    "tsBuildInfoFile": "../../../build/test/tsconfig.tsbuildinfo",
    "rootDir": ".",
    // adding types for mocha here causes languageProcessor.tests.js
    // (which isn't in typescript) to fail
    // "types": [
    //   "mocha",
    //   "chai"
    // ]
  },

  "include": [
    "dom/**/*.ts",
    "headless/**/*.ts",
    "integrated/**/*.ts",
    "resources/@types/*.d.ts",
    "predictive-text/**/*.ts",
  ],

  "references": [
    { "path": "../../engine" },
    { "path": "resources" },
    { "path": "headless-resources" },
  ]
}
